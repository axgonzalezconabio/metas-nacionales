import 'package:flutter/material.dart';

class EjesPage extends StatelessWidget {
  const EjesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Explorar por ejes'),
      ),
      body: const Center(
        child: Text('Explorar por ejes'),
      ),
    );
  }
}